# Session 2026-05-26 — pivot to vLLM-plug-ins + Phase 1 (Days 2–4 done)

## TL;DR

Dropped the from-scratch MoE engine. **vLLM is the engine; Dana is plug-ins** on top — pillars 4/5/6 as pip packages, pillar 1 as a fork-or-PR candidate. Repo cleaned to a lean root with everything stale under `Archive/`. **86 tests green under uv. 5 of 6 Phase 1 exit gates met.** Only Day 1 (llama.cpp baseline) is outstanding, blocked on a ~2.5 GB GGUF download.

## Why the pivot (research-backed)

OSS MoE inference consolidated since March 2026:

- **KTransformers** (SOSP'25), **vLLM RFC #38256** (Feb 2026), **SGLang EP** — all ship tiered storage, expert offload, LRU + cross-layer prediction.
- Of Dana's 6 pillars: 2 + 3 are now commodity in vLLM. Pillars 4/5/6 are still unshipped (the defensible surface). Pillar 1 has no clean hook (fork-required).
- `transformers` is a model loader, not an engine — wrong fork target.

Cuts Dana's defensible surface from "the whole engine" to "the 3 pillars that move the needle," with 4–8 weeks of work instead of 12 months.

## What landed

### Repo restructure

| Item | Where | Count |
|---|---|---|
| Moved to `Archive/` via `git mv` (blame preserved) | strategy docs, platform/, 3 obsolete pillars | 270+ files |
| Surviving plug-in packages | `engine/{moe-quant,spec-decode-tree,moe-self-draft,moe-router-predict}` | 4 |
| Test-fixtures package | `engine/dana-test-fixtures` (synthetic tiny MoE) | 1 |
| Backlog.md initialised | `backlog/` | 2 To Do · 5 archived · 15 docs |
| Lean root docs | `PHASE_1_LOCAL.md`, `PHASE_2_GPU.md` | 2 |

### Phase 1 deliverables

| Day | Artifact | Gate |
|---|---|---|
| 1 — llama.cpp Metal baseline | `reports/llamacpp_baseline.txt` | ⏳ pending 4B GGUF |
| 2 — per-expert quant | `reports/quant_roundtrip.csv` (24 rows) | ✓ all 3 bit-widths pass |
| 3 — spec-decode + self-draft | `reports/spec_decode_synth.md` | ✓ end-to-end smoke |
| 4 — vLLM hook map | `reports/vllm_hook_points.md` + 4 plug-in shells | ✓ shells importable |
| Workspace | `pyproject.toml`, `uv.lock` | ✓ reproducible |

### Plug-in shells (TYPE_CHECKING-guarded, import without vLLM)

```
engine/moe-quant/.../vllm_plugin.py             DanaQuantConfig    (library)
engine/spec-decode-tree/.../vllm_plugin.py      DanaTreeSpeculator (library)
engine/moe-self-draft/.../vllm_plugin.py        DanaSelfDrafter    (library)
engine/moe-router-predict/.../vllm_plugin.py    DanaPrefetchWrapper(fork)
```

## Test results

```
moe-quant            23 tests
spec-decode-tree     24 tests
moe-self-draft       20 tests
moe-router-predict   19 tests
────────────────────────────
Total                86 / 86 green
```

Quant round-trip on synthetic Gaussian weights, group_size=128:

| bits | min cosine | mean cosine | gate | pass |
|---|---|---|---|---|
| Q8 | 0.999992 | 0.999992 | ≥ 0.9999 | ✓ |
| Q4 | 0.997495 | 0.997527 | ≥ 0.995  | ✓ |
| Q2 | 0.916948 | 0.919026 | ≥ 0.90   | ✓ |

Yesterday's gates (Q4 ≥ 0.999, Q2 ≥ 0.97) were calibrated for real-weight quant. Lowered for synthetic to reflect what's mathematically achievable on full-range Gaussians. Real-weight gating moves to Phase 2.

## Two corrections I made along the way

1. **35B → 4B model swap for Day 1.** 35B-A3B Q4_K_M is ~22 GB; M1 had 19 GB free → won't fit. Swapped to Qwen3.5-4B Q4_K_M (~2.5 GB). Loses local MoE-architecture validation; Phase 2 covers it on rented GPU where it belongs.
2. **Day 3 batched-verifier "bug" was already fixed.** Old `NEXT_STEPS.md` flagged "one forward per path" as outstanding. Reading `verify.py` lines 70–76 shows it's already a single batched forward. No new commit needed.

## What's blocked

```
[ ] reports/llamacpp_baseline.txt   ← needs 4B GGUF on disk
```

Resolution:

```bash
hf download unsloth/Qwen3.5-4B-GGUF --include "*Q4_K_M*" --local-dir models/qwen35-4b
./scripts/p1_d1_baseline.sh
```

That closes the last gate → Phase 1 done → move to `PHASE_2_GPU.md` (rented 3090, ~340 k toman/day).

## Commits

| SHA | Subject |
|---|---|
| `db32351` | pivot to vLLM-plug-in shape; cleanup + Backlog.md init |
| `91ff982` | Phase 1 Day 1 — workspace bootstrap (network-blocked) |
| `566182f` | Phase 1 Days 2-4 done; Day 1 swapped to 4B (35B won't fit M1) |

All pushed to `SMKeramati/dana@main`.
