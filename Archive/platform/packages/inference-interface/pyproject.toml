[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "inference-interface"
version = "0.1.0"
description = "Engine adapter contract — shared by all inference backends"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/inference_interface"]
